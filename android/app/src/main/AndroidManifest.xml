<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Requerida para TODAS las llamadas de red (Supabase, mapas, etc.).
         Sin esto, los builds release/profile fallan con
         "SocketException: Failed host lookup" en la primera llamada de red
         (el login). El manifest de debug ya la incluye, por eso el error
         solo aparece fuera de debug. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- ===================================================================
         BLUETOOTH / BLE - preparado para la integracion con el ESP32.
         La app es CENTRAL (escanea y se conecta); el ESP32 es el periferico
         que anuncia. Se declaran aqui para que el manifest ya no cambie
         cuando se agregue el paquete de BLE al proyecto.
         =================================================================== -->

    <!-- Android 11 (API 30) y anteriores: modelo viejo de permisos. -->
    <uses-permission android:name="android.permission.BLUETOOTH"
        android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN"
        android:maxSdkVersion="30" />

    <!-- Android 12 (API 31) en adelante: permisos nuevos, se piden en runtime.
         `neverForLocation` declara que NO derivamos ubicacion del escaneo, asi
         el sistema no exige permiso de ubicacion solo para escanear.
         OJO: ACCESS_FINE_LOCATION de arriba se queda SIN maxSdkVersion porque
         la app si la necesita de verdad (mapas y geolocalizacion). -->
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation"
        tools:targetApi="s" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />

    <!-- required=false: la app sigue instalable en equipos sin BLE. -->
    <uses-feature android:name="android.hardware.bluetooth_le"
        android:required="false" />
    <uses-feature android:name="android.hardware.bluetooth"
        android:required="false" />

    <application
        android:label="BIO-G"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">

        <!-- ✅ GOOGLE MAPS KEY (AQUÍ ADENTRO) -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyCujhe2xUFhzPkfgj6PkQgTwfn2fcqHwBA" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">

            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
</manifest>
